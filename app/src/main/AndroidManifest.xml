<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".view.RepairsActivity"></activity>
        <activity android:name=".presenter.acivity.enter_password_acivity" />
        <activity android:name=".presenter.acivity.Visitors_acivity" />
        <activity android:name=".presenter.acivity.Questionnaire_acivity" />
        <activity android:name=".presenter.acivity.Inform_acivity" />
        <activity android:name=".presenter.acivity.Repairs_acivity" />
        <activity android:name=".presenter.acivity.Binding_acivity" /> <!-- <meta-data android:name="com.google.android.actions" /> -->
        <activity android:name=".presenter.acivity.Find_acivity" />
        <activity android:name=".presenter.acivity.Change_acivity" />
        <activity android:name=".presenter.acivity.Start_acivity" />
        <activity android:name=".presenter.acivity.Verification_acivity" />
        <activity android:name=".view.Entrance_GuardActivity"></activity>
        <activity android:name=".presenter.acivity.Landing_acivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>